name: Manage doc versions

on:
  push:
    branches:
      - gh-pages
    paths:
      - 'v[0-9]+.[0-9]+/.build'

jobs:
  doc-versions:
    name: Handle new version of docs
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 10
    - name: Update versions.json
      run: ls *

    - name: Update latest link
      run: echo 'Update link'
